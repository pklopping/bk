// Generated by CoffeeScript 2.5.1
(function() {
  var resizeVideos;

  $(function() {
    var timer;
    resizeVideos();
    timer = 0;
    return $(window).on("resize", function() {
      clearTimeout(timer);
      return timer = setTimeout(function() {
        return resizeVideos();
      }, 300);
    });
  });

  resizeVideos = function() {
    var $video, $videos, height, i, len, ratio, real_width, results, video, width;
    $videos = $('iframe');
    results = [];
    for (i = 0, len = $videos.length; i < len; i++) {
      video = $videos[i];
      $video = $(video);
      width = $video.attr('width');
      height = $video.attr('height');
      real_width = $video.width();
      ratio = height / width;
      results.push($video.css('height', ratio * real_width));
    }
    return results;
  };

}).call(this);
