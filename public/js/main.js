// Generated by CoffeeScript 1.7.1
(function() {
  var resizeVideos;

  $(function() {
    var timer;
    resizeVideos();
    timer = 0;
    return $(window).on("resize", function() {
      clearTimeout(timer);
      return timer = setTimeout(function() {
        return resizeVideos();
      }, 300);
    });
  });

  resizeVideos = function() {
    var $video, $videos, height, ratio, real_width, video, width, _i, _len, _results;
    $videos = $('iframe');
    _results = [];
    for (_i = 0, _len = $videos.length; _i < _len; _i++) {
      video = $videos[_i];
      $video = $(video);
      width = $video.attr('width');
      height = $video.attr('height');
      real_width = $video.width();
      ratio = height / width;
      _results.push($video.css('height', ratio * real_width));
    }
    return _results;
  };

}).call(this);
